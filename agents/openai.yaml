interface:
  display_name: "Adaptive Subagent Orchestrator"
  short_description: "Route local, offloaded, parallel, or bundled heavy-goal work safely"
  default_prompt: "Use $adaptive-subagent-orchestration in balanced mode unless host policy explicitly selects compute-offload; choose the smallest safe route, hand L3 to the bundled heavy workflow, and integrate verified results."

policy:
  allow_implicit_invocation: true
