interface:
  display_name: "Adaptive Subagent Orchestrator"
  short_description: "Route medium tasks through 1-3 independent subagents"
  default_prompt: "Use $adaptive-subagent-orchestration to assess this task, create only worthwhile independent lanes, and integrate verified results."

policy:
  allow_implicit_invocation: true
